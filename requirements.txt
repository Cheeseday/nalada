# ─────────────────────────────────────────────────────────────────────────────
# Web-app runtime dependencies: the subset the Flask app imports at request time,
# pinned and pip-installable for a Render (or any pip-based) deploy.
#
# This is NOT the full local/notebook environment - that lives in environment.yml
# (`conda env create -f environment.yml`). Only what app/ and db/ need to serve
# pages is here, so the deployed image stays lean.
# ─────────────────────────────────────────────────────────────────────────────
Flask==3.1.3
SQLAlchemy==2.0.49
psycopg2-binary>=2.9.9,<2.10   # Postgres driver; the -binary build ships wheels (no compiler needed on Render)
pandas==3.0.3
numpy==2.4.3
plotly==6.8.0
python-dotenv==1.2.2
gunicorn==23.0.0               # production WSGI server (Linux only; local dev still uses flask run)
